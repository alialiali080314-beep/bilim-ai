# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Project Status

This repository (`bilim-ai`) is newly initialized and contains no source code yet. This file should be updated once the project structure, tooling, and conventions are established.

When the project is set up, add the following sections:

- **Build & dev commands** — how to install dependencies, start the dev server, build for production
- **Test commands** — how to run the full test suite and a single test file/case
- **Lint & format commands** — the linter and formatter in use and how to run them
- **Architecture overview** — the high-level structure: what the major modules are, how data flows between them, and any non-obvious design decisions
- **Key conventions** — naming patterns, file organization rules, or framework-specific constraints that aren't obvious from the code alone
